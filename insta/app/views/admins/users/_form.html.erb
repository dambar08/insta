<%= form_with(model: admins_user) do |form| %>
  <% if admins_user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admins_user.errors.count, "error") %> prohibited this admins_user from being saved:</h2>

      <ul>
        <% admins_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
